{
    "$schema": "https://json.schemastore.org/tsconfig",

    "extends": "../tsconfig-base.json",
    "compilerOptions": {
        /* emit */
        "outDir": "../../bin/"
    },
    "references": [ 
        { "path": "../lib" }
    ]
}
