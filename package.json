{
    "name": "paint-io-teams-server",
    "scripts": {
        "build": "mkdir -p src/generated && echo \"{\\\"version\\\":\\\"$(git describe --always --dirty 2>/dev/null)\\\"}\" > src/generated/version.json; tsc -b",
        "start": "node dist/index.js"
    },
    "dependencies": {
        "socket.io": "^4.3.1"
    },
    "devDependencies": {
        "typescript": "^4.4.4"
    }
}
